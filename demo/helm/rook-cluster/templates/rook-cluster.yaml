apiVersion: rook.io/v1alpha1
kind: Cluster
metadata:
  name: {{ template "name" . }}
  namespace: {{ .Release.Namespace }}
  label:
    rook_cluster: {{ template "name" . }}
spec:
{{ toYaml .Values.cluster | indent 2 }}
