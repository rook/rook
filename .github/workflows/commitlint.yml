name: Commitlint
on:
  push:
    tags:
      - v*
    branches:
      - master
      - release-*
  pull_request:
    branches:
      - master
      - release-*

# cancel the in-progress workflow when PR is refreshed.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.head_ref || github.sha }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-20.04
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
<<<<<<< HEAD
      - uses: wagoid/commitlint-github-action@v5.3.0
=======
      - uses: wagoid/commitlint-github-action@v5.4.3
>>>>>>> d5f5f0b64 (build(deps): bump wagoid/commitlint-github-action from 5.4.1 to 5.4.3)
        with:
          configFile: "./.commitlintrc.json"
          helpURL: https://rook.io/docs/rook/latest/Contributing/development-flow/#commit-structure
